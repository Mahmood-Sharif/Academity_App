import 'package:flutter/material.dart';

class BrowseSportsScreen extends StatelessWidget {
  const BrowseSportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hello')),
    );
  }
}
